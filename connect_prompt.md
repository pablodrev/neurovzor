Role: Ты Senior Fullstack Developer и эксперт по системной интеграции. Твоя задача — соединить Frontend (React.js) и Backend (FastAPI), которые разрабатывались независимо.

Context: Мы разрабатываем MedTech-платформу для диагностики дисплазии ТБС.

Backend: FastAPI, Python, выполняет ML-анализ (YOLO + сегментация) и возвращает геометрические расчеты (углы, линии).

Frontend: React, использует Cornerstone3D или SVG для визуализации снимков и результатов ИИ.

Step 1: Deep Analysis
Тщательно изучи файлы в папках /frontend и /backend. Проверь следующие аспекты и сообщи мне списком о любых неточностях или неоднозначностях, прежде чем предлагать код интеграции:

API Endpoints: Соответствуют ли URL-пути на фронтенде роутам в FastAPI?

Schema Mismatch: Совпадают ли названия полей и типы данных в Pydantic-схемах бэкенда с тем, что ожидает фронтенд (например, acetabular_index vs acetabularIndex)?

Coordinates System: В каком формате передаются точки (пиксели, проценты от ширины, или координаты DICOM)? Нет ли здесь противоречий?

Auth & Headers: Какие заголовки (Content-Type, Authorization) требуются бэкенду и отправляет ли их фронтенд?

Step 2: Integration Tasks
После аудита помоги мне реализовать следующее:

CORS Configuration: Настрой CORSMiddleware в FastAPI, чтобы разрешить запросы с локального адреса фронтенда.

API Client: Создай или обнови сервис на фронтенде (через axios или fetch), который будет загружать изображения как FormData и правильно обрабатывать сложный JSON-ответ с масками и точками.

Error Handling: Настрой на фронтенде обработку типичных ошибок FastAPI (например, ошибку валидации 422 или 500 при сбое ML-модели).

Metadata Alignment: Убедись, что возраст пациента извлекается из DICOM на бэкенде и корректно передается на фронтенд для сравнения с нормами.

Output Format:

Сначала выведи раздел "Обнаруженные расхождения".

Затем предложи исправления для Backend.

Затем предложи код для Frontend.
